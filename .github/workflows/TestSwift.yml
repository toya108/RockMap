name: Test Swift

on: [push]

jobs:
  test:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v2
      - name: Run tests
        run: |
          xcodebuild test -project RockMap.xcodeproj -scheme RockMap -destination 'platform=iOS Simulator, name=iPhone 12, OS=14.5'
      - name: Run tests
        run: swift test
